# Minimal Python dependencies for the normalization & inspection notebooks
# Use a virtual environment (venv/conda) and install with:
# python -m pip install -r requirements.txt

numpy>=1.24
pandas>=1.5
librosa>=0.10
soundfile>=0.12
requests>=2.28
tqdm>=4.65
python-dotenv>=1.0
supabase>=1.0
matplotlib>=3.6
ipython>=8.0

# Optional (for VAD-based trimming). NOTE: building on Windows requires
# Microsoft C++ Build Tools (or use pre-built wheel if available).
webrtcvad>=2.0

# System dependency: ffmpeg binary (install via system package manager)
# On Windows: winget install --id=Gyan.FFmpeg -e  (or add ffmpeg to PATH)
